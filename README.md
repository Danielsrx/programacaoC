# Super Trunfo de Cidades - Desafio C

Este projeto é uma implementação do jogo Super Trunfo de Cidades, desenvolvido em C como parte de um desafio de programação. O programa permite ao usuário cadastrar duas cartas (cidades), calcula atributos derivados e, por fim, compara as duas cartas para determinar a vencedora em cada categoria.

Este projeto cobre os seguintes conceitos da linguagem C:
* Entrada e Saída Padrão (`printf`, `scanf`)
* Tipos de Dados Primitivos (`int`, `float`, `char`)
* Tipos de Dados Derivados (`char[]` ou strings)
* Modificadores de Tipo (`unsigned long int`)
* Operações Aritméticas
* Conversão de Tipos (Casting)
* Operadores Relacionais e o Operador Ternário

---

## 🚀 Como Compilar e Executar

Este programa foi escrito em C padrão e pode ser compilado com qualquer compilador C moderno, como o `gcc`.

### 1. Compilação

Após clonar o repositório ou salvar o arquivo `super_trunfo.c` em seu computador, abra um terminal e navegue até a pasta onde o arquivo está.

Execute o seguinte comando para compilar o programa:

```bash
gcc super_trunfo.c -o super_trunfo
